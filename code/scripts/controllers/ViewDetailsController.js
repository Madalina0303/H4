const {WebcController} = WebCardinal.controllers;

export default class ViewDetailsController extends WebcController {
    constructor(...props) {

        super(...props);
 
        this.model={}
 
    }
}